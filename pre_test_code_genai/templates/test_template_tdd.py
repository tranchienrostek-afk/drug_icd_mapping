# Mẫu file test rỗng cho AI
import pytest
# from app.services import your_service

def test_feature_scenario_expected_behavior():
    """
    RED PHASE: Mô tả hành vi mong muốn.
    Ví dụ: Input là A -> Output phải là B.
    """
    # 1. Arrange (Chuẩn bị dữ liệu)
    input_data = "..."
    
    # 2. Act (Gọi hàm cần test - hàm này có thể chưa tồn tại)
    # result = your_service.process(input_data)
    
    # 3. Assert (Kiểm tra kết quả)
    # assert result == "expected_output"
    assert True # Sửa thành False để bắt đầu Loop